{:paths ["src"]
 ;; Pure .cljc, no deps: the rubric must be loadable from a browser build, an
 ;; nbb script, or a JVM test run without dragging a runner in behind it.
 :deps {}
 :aliases {:test {:extra-paths ["test"]}}}
